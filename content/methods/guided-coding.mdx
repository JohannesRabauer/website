---
title: "Guided Coding (Archived Specs / ADR)"
tagline: "Write a guide for the change, then file it away as a record."
category: "Spec-Driven"
icon: "archive"
maturity: "Emerging"
order: 5
diagram:
  type: flow
  variant: linear
  title: "One change, one guide, one record."
  steps:
    - label: "Write the guide"
      description: "State the goal and the limits."
    - label: "Agent implements"
      description: "Work inside those limits."
    - label: "Review and merge"
      description: "Check the change against the guide."
    - label: "Archive the record"
      description: "Keep the reason, not the draft."
codeExamples:
  title: "Guided Coding in three minimal steps"
  subtitle: "Guide first, implementation second, decision record last."
  steps:
    - title: "Step 1 — Write a focused guide"
      goal: "Define goal and constraints before coding."
      java: |-
        record ChangeGuide(
            String goal,
            List<String> constraints,
            List<String> outOfScope) {}
      agnostic: |-
        guide:
          goal: add transfer endpoint
          constraints: [keep package boundaries, no schema change]
          out_of_scope: [new auth model]
      check: "The guide sets clear boundaries for the agent."
    - title: "Step 2 — Implement against the guide"
      goal: "Validate the change with the guide's acceptance checks."
      java: |-
        @Test
        void transferEndpointMatchesGuide() {
          assertThat(api.transfer(alice, bob, euros(25)).status()).isEqualTo(200);
        }
      agnostic: |-
        implement according to guide
        run acceptance test for transfer endpoint
      check: "Behavior matches the guide, not assumptions."
    - title: "Step 3 — Archive the decision"
      goal: "Keep the reasoning as a durable team record."
      java: |-
        String adr = """
        # ADR-042 Transfer endpoint
        Decision: keep existing auth boundary.
        Rationale: minimize blast radius for this change.
        """;
      agnostic: |-
        archive adr:
          id: ADR-042
          decision: keep existing auth boundary
          rationale: minimize blast radius
      check: "The why is preserved for future changes."
sources:
  - label: "Guided Coding PDF"
    href: "https://github.com/JohannesRabauer/vaadin-banking-app/blob/main/2026-02-26-guided-coding-instead-of-vibe-coding-in-java.pdf"
  - label: "Guided Coding blog post"
    href: "https://rabauer.dev/en/blog/guided-coding/"
inventor:
  name: "Kenny Pflug"
  github: "feO2x"
relatedPosts:
  - "guided-coding"
draft: false
---

## What it is

Guided Coding keeps the plan, the limits, and the review rules visible. You write the guide
before the code. The guide tells the agent what to do and what not to do.

## Why it matters

This keeps the human in control. It also makes review easier, because you compare the code against
the guide instead of against memory.
