---
title: "AI Unified Process"
tagline: "Put the business spec first. Let AI build from it in four phases."
category: "Spec-Driven"
icon: "layers"
maturity: "Emerging"
order: 1
diagram:
  type: phases
  title: "Hover a phase to see the artifacts it creates."
  phases:
    - label: "Inception"
      summary: "Agree on the problem."
      artifacts:
        - "Vision"
        - "Requirements"
        - "Test Strategy"
    - label: "Elaboration"
      summary: "Shape the system."
      artifacts:
        - "Entity Model"
        - "System Use Cases"
        - "Architecture"
        - "Acceptance Tests"
    - label: "Construction"
      summary: "Build from the spec."
      artifacts:
        - "Code"
        - "Reviews"
        - "Test results"
    - label: "Transition"
      summary: "Ship and verify."
      artifacts:
        - "Deployment"
        - "User Acceptance Tests"
codeExamples:
  title: "AIUP in three minimal steps"
  subtitle: "Keep artifacts explicit, then let implementation follow."
  steps:
    - title: "Step 1 — Capture Inception output"
      goal: "State the business problem and expected outcome."
      java: |-
        record Vision(String problem, String outcome) {}

        interface InceptionPhase {
          Vision defineVision();
        }
      agnostic: |-
        phase Inception:
          output vision { problem, outcome }
      check: "The vision is explicit and reviewable."
    - title: "Step 2 — Shape Elaboration artifacts"
      goal: "Name a core use case before writing implementation details."
      java: |-
        record UseCase(String name, String actor, String outcome) {}

        UseCase transferUseCase =
            new UseCase("Transfer money", "Customer", "Money moved safely");
      agnostic: |-
        phase Elaboration:
          define use_case "Transfer money"
          actor: Customer
          result: Money moved safely
      check: "A concrete use case exists before coding."
    - title: "Step 3 — Build and verify Transition"
      goal: "Implement from the spec and verify end-to-end behavior."
      java: |-
        Receipt receipt = transferService.transfer(alice, bob, euros(25));
        assertThat(receipt.success()).isTrue();
        acceptance.verify("Transfer works end-to-end");
      agnostic: |-
        run transfer(alice, bob, 25)
        assert success == true
        run acceptance_check "Transfer works end-to-end"
      check: "Code and acceptance checks both pass."
sources:
  - label: "AI Unified Process methodology"
    href: "https://unifiedprocess.ai/methodology.html"
  - label: "AIUP marketplace"
    href: "https://github.com/AI-Unified-Process/marketplace"
inventor:
  name: "Simon Martinelli"
  github: "simasch"
relatedPosts:
  - "ai-unified-process"
draft: false
---

## What it is

AI Unified Process is a spec-first workflow. You write the business intent first. AI then turns
that intent into architecture, code, tests, and release work.

It uses four phases: Inception, Elaboration, Construction, and Transition. Each phase has its own
outputs and its own purpose.

## Why it matters

The spec stays in charge. That makes change easier to manage. When the requirements change, you
update the spec and let AI rebuild the parts below it.
