---
title: "BMAD Method"
tagline: "Pick the track that fits the job, then move from analysis to implementation with specialized agents."
category: "Agent Framework"
icon: "users"
maturity: "Established"
order: 3
diagram:
  type: tracks
  title: "Three tracks, one method."
  tracks:
    - key: "quick"
      label: "Quick Flow"
      summary: "Small jobs need less ceremony."
      bestFor: "Bug fixes and small features"
      steps:
        - "Tech spec"
        - "Implement"
        - "Check"
    - key: "bmad"
      label: "BMad Method"
      summary: "Plan first, then work story by story."
      bestFor: "Products and complex features"
      steps:
        - "Analysis"
        - "Planning"
        - "Solutioning"
        - "Implementation"
    - key: "enterprise"
      label: "Enterprise"
      summary: "Add more depth for larger systems."
      bestFor: "Regulated or multi-team work"
      steps:
        - "PRD"
        - "Architecture"
        - "Security"
        - "DevOps"
        - "Stories"
codeExamples:
  title: "One method, three tracks"
  subtitle: "Swipe to see how each planning track looks in practice."
  steps:
    - title: "Quick Flow"
      goal: "Bug fix or small feature — minimal ceremony."
      java: |-
        // Tech spec (one short comment block)
        // Goal: fix transfer rounding error
        // Acceptance: result is always two decimal places

        BigDecimal amount = value.setScale(2, RoundingMode.HALF_UP);
        assertThat(service.transfer(alice, bob, amount).success()).isTrue();
      agnostic: |-
        tech_spec:
          goal: fix transfer rounding error
          acceptance: result is always two decimal places

        implement fix
        run acceptance test
      check: "Spec and test fit in one block. No planning ceremonies needed."
    - title: "BMad Method"
      goal: "New product feature — plan before implementing."
      java: |-
        // Analysis: what does the user need?
        Story story = new Story("ST-42", "Transfer money", "funded transfer succeeds");

        // Planning: break into tasks
        List<String> tasks = List.of("validate input", "debit account", "credit account");

        // Implementation (per task, tested before next)
        Receipt receipt = service.transfer(alice, bob, euros(25));
        assertThat(receipt.success()).isTrue();
      agnostic: |-
        analysis: customer needs reliable transfer
        story ST-42:
          goal: transfer money
          acceptance: funded transfer succeeds

        tasks: [validate, debit, credit]
        implement each task with tests
      check: "Story acceptance drives implementation. Agent stays in scope."
    - title: "Enterprise"
      goal: "Complex or regulated system — add architecture and security layers."
      java: |-
        // PRD: product requirements documented
        // Architecture: component diagram updated
        // Security: threat model reviewed for transfer path

        @PreAuthorize("hasRole('TRANSFER')")
        Receipt transfer(Account from, Account to, Money amount) {
            auditLog.record(from, to, amount);
            return core.transfer(from, to, amount);
        }
      agnostic: |-
        prd: requirements documented
        architecture: component diagram updated
        security: threat model reviewed

        enforce authorization on transfer path
        record audit trail
        run full acceptance + security tests
      check: "Governance gates are defined before implementation starts."
sources:
  - label: "BMad Method docs"
    href: "https://docs.bmad-method.org/"
  - label: "BMAD-METHOD on GitHub"
    href: "https://github.com/bmad-code-org/BMAD-METHOD"
  - label: "BMad Builder docs"
    href: "https://bmad-builder-docs.bmad-method.org/"
inventor:
  name: "Brian (BMadCode)"
  github: "bmadcode"
draft: false
---

## What it is

BMAD means Build More, Architect Dreams. It is a framework of specialized agents and guided
workflows. BMad-Help points you to the next step. You do not have to guess the path.

## Pick the right track

Quick Flow fits small jobs. BMad Method fits larger products. Enterprise adds more planning for
harder systems. The point is not to use every phase. The point is to use the right one.
